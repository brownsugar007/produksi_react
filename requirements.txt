# Core dependencies (Vercel Serverless Functions)
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
requests>=2.31.0
msal>=1.26.0
fastapi>=0.100.0
numpy>=1.24.0

# Note: streamlit, uvicorn, gunicorn are NOT needed on Vercel
# They are only needed for local development
